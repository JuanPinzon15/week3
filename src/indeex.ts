class player {
    team: string;
    position: number;

    constructor(team: string, position: number) {
        this.team = team;
        this.position = position;
    }

    public getPosition(): number {
    }
}

logObject(user);